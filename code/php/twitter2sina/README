

1. run `php token.php` in command line to get your access token.

2. get a app key from http://open.t.sina.com.cn/apps. change $source to your app key.

3. change $twitterAuth and $sinaAuth

4. add a entry like `*/1 * * * * /usr/bin/php /home/xp/twitter2sina/sync.php` to crontab.


